<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ====== الأذونات الأساسية ====== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- ====== أذونات الكاميرا والميكروفون ====== -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- ====== أذونات الخدمات الخلفية والإشعارات ====== -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- تم إزالة FOREGROUND_SERVICE_MEDIA_PROJECTION لأنها غير ضرورية -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- ====== أذونات التخزين (للتوافق مع Android 13+) ====== -->
    <!-- تم إزالة READ_EXTERNAL_STORAGE و WRITE_EXTERNAL_STORAGE نهائياً -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    
    <!-- ====== تم حذف الأذونات الزائدة: WAKE_LOCK, VIBRATE, MODIFY_AUDIO_SETTINGS ====== -->

    <!-- ====== ميزات الجهاز المطلوبة ====== -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.wifi"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:allowClearUserData="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/ic_launcher"
        android:label="Shield Core"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning"
        tools:targetApi="33">

        <!-- ====== النشاط الرئيسي ====== -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ====== تعريفات الخدمات المطلوبة ====== -->
        <!-- خدمة تحليل الكاميرا (تعمل في الخلفية) -->
        <service
            android:name=".CameraAnalyzer"
            android:exported="false"
            android:foregroundServiceType="camera" />

        <!-- خدمة إدارة التسجيل والبث (إذا كانت موجودة) -->
        <service
            android:name=".StreamManager"
            android:exported="false"
            android:foregroundServiceType="microphone|camera" />

    </application>
</manifest>
