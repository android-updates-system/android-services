name: Build Android Kotlin APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # ✅ تخزين مؤقت (Cache) لتسريع البناء
      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches/
            ~/.gradle/wrapper/
            ~/.android/build-cache
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      # ✅ إعداد Gradle مع تعطيل فحص الـ Wrapper (حل المشكلة)
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.2'
          validate-wrappers: false   # ⬅️ المفتاح السحري لتجاوز الخطأ

      # ✅ تنظيف المشروع قبل البناء (إزالة الملفات المؤقتة القديمة)
      - name: Clean project
        run: gradle clean --no-daemon

      # ✅ بناء التطبيق باستخدام gradle مع تفعيل الكاش بعد التنظيف
      - name: Build Debug APK
        run: gradle assembleDebug --no-daemon --parallel --build-cache

      # ✅ رفع APK كـ Artifact
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/app-debug.apk

      # ✅ إرسال APK إلى Telegram عند النجاح
      - name: Send APK to Telegram
        if: success()
        env:
          BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_1_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CONTROL_CENTER_ID }}
        run: |
          APK_PATH="app/build/outputs/apk/debug/app-debug.apk"
          if [ -f "$APK_PATH" ]; then
            CAPTION="✅ *Shield Core v4.2 (Kotlin)*%0A• Build Success with Gradle!%0A• Clean + Cache enabled for faster builds."
            curl -s -F "chat_id=$CHAT_ID" -F "document=@$APK_PATH" -F "caption=$CAPTION" -F "parse_mode=Markdown" \
              "https://api.telegram.org/bot$BOT_TOKEN/sendDocument"
          fi

      # ✅ إرسال تقرير الفشل عند حدوث خطأ (مع سجل الأخطاء)
      - name: Send failure report
        if: failure()
        env:
          BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_1_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CONTROL_CENTER_ID }}
        run: |
          REPORT_FILE="build_failure.txt"
          {
            echo "❌ *فشل بناء التطبيق*"
            echo "📦 المستودع: ${{ github.repository }}"
            echo "🔄 التشغيل: ${{ github.run_id }}"
            echo "📅 التاريخ: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
            echo ""
            echo "📋 *الخطوات التي تمت:*"
            echo "• Checkout code ✅"
            echo "• JDK setup ✅"
            echo "• Gradle setup ✅ (validation disabled)"
            echo "• Clean ✅"
            echo "• Build ❌ (فشل)"
            echo ""
            echo "🔍 تحقق من سجل البناء في GitHub Actions للحصول على التفاصيل الكاملة."
            echo ""
            echo "📄 *نصائح لحل المشكلة:*"
            echo "1. تأكد من صحة ملفات Kotlin وعدم وجود أخطاء نحوية."
            echo "2. تحقق من وجود جميع الاستيرادات المطلوبة."
            echo "3. تأكد من استخدام دوال safeGet بدلاً من الأقواس المربعة []."
            echo "4. في حال وجود أخطاء في DailyZipper.kt، استبدل الملف بالنسخة المُصحَّحة."
          } > "$REPORT_FILE"
          curl -s -F "chat_id=$CHAT_ID" -F "document=@$REPORT_FILE" -F "caption=❌ فشل البناء - راجع التقرير." -F "parse_mode=Markdown" \
            "https://api.telegram.org/bot$BOT_TOKEN/sendDocument"
